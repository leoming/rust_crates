# riscv-rt-macro has local patches to upgrade dependencies. Capture that here.
[package]
name = "riscv-rt-macros-deps"
version = "0.1.0"
authors = []
edition = "2018"

[dependencies]
proc-macro2 = "1"
quote = "1"
rand = { version = "0.7.3", features = ["small_rng"] }
syn = "1"
